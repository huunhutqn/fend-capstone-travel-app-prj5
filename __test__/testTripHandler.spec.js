import { tripHandler } from "../src/client/js/tripHandler";

describe("Testing change departing time", () => {
  test("Testing the tripHandler() function", () => {
    expect(tripHandler).toBeDefined();
  });
});
